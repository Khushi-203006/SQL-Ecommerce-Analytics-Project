-- Using recursive CTE, create a product category hierarchy (if categories have parent-child relationships)

/*
Since the dataset does not contain hierarchical category data with parent-child relationships,
a recursive CTE cannot be implemented directly.
*/
